{
  "name": "react-hooks-redux-reducer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "echo \".results.json\" && jest --watchAll"
  },
  "devDependencies": {
    "@babel/core": "^7.12.9",
    "@babel/preset-env": "^7.12.7",
    "@learn-co-curriculum/jest-learn-reporter": "^0.1.0",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0",
    "mocha": "^11.3.0"
  },
  "jest": {
    "reporters": [
      "@learn-co-curriculum/jest-learn-reporter",
      "default"
    ],
    "rootDir": "src"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  }
}
